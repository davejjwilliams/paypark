<h2 class="panel-title">Drivers who have booked with you:</h2>
<ul>
  <% @homeowner_conversations_with.each do |user| %>
    <li><%= link_to User.find(user.user_id).name, conversations_path(user_id: user), remote: true, method: :post %></li>
  <% end %>
</ul>
