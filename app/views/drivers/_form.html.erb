<%= form_with(model: driver, local: true) do |form| %>
  <% if driver.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(driver.errors.count, "error") %> prohibited this driver from being saved:</h2>

      <ul>
      <% driver.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" id="driver_new_mid">
    <%= form.label :please_enter_your_registration_number %>
    <br>
    <%= form.text_field(:registration_number, :class => 'input', :id => "driver_field") %>
  </div>

  <div class="actions" id="driver_new_bot">
    <%= form.submit({class: "button is-primary"}) %>
  </div>
<% end %>
