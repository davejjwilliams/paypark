<%= form_with(model: withdrawal_request, local: true) do |form| %>
  <% if withdrawal_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(withdrawal_request.errors.count, "error") %> prohibited this withdrawal_request from being saved:</h2>

      <ul>
      <% withdrawal_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" style="text-align: center">
    <%= form.label :homeowner_id, :class => "subtitle" %>
    <br>
    <%= form.text_field :homeowner_id, :class => "input", :style => "width: 50%" %>
  </div>

  <div class="field" style="text-align: center">
    <%= form.label :amount, :class => "subtitle" %><br>

    <%= form.text_field :amount, :class => "input", :style => "width: 50%" %>
  </div>

  <div class="field" style="text-align: center">
    <%= form.label :request_date, :class => "subtitle" %>
    <%= form.date_select :request_date %>
  </div>
  <br><br>

  <div class="actions" style="text-align: center">
    <%= form.submit :class => "button is-primary" %>
  </div>
<% end %>
